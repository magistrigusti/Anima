[project]
name = "anima"
version = "0.1.0"
requires-python = ">=3.12,<3.14"
dependencies = [
  "httpx==0.24.1",
  "python-dotenv==1.0.1",
  "python-telegram-bot==20.3",
]

[tool.vercel]
entrypoint = "api.index:handler"
